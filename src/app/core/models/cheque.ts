export interface ChequeList {
    series: string, 
    start: number, 
    end: number, 
    quantity: number 
}
export interface NewChequeEntry {
    series: string, 
    start: number, 
    end: number,
}
